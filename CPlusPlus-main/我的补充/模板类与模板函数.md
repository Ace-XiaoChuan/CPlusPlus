```cpp
rclcpp::Publisher<geometry_msgs::msg::Twist>::SharedPtr publisher_;
```
<> 是 模板参数列表，用于指定模板的具体类型。
这是一个模板类 Publisher<T>，而这里的 T = geometry_msgs::msg::Twist。
意思是：“我要创建一个发布 Twist 类型消息的 Publisher。”
这样我在使用这个类时我就知道应该传入什么类型的参数（此处是指应该传入Twist类型的参数）